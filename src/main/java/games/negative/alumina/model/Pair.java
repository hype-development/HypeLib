package games.negative.alumina.model;

/**
 * Represents a generic pair of left and right values.
 *
 * @param <L> The type of the left value.
 * @param <R> The type of the right value.
 */
public record Pair<L, R>(L left, R right) {

}
