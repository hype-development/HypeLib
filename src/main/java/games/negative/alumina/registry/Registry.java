package games.negative.alumina.registry;

import java.util.Map;

/**
 * A registry is a map used to store objects.
 * @param <K> The key type.
 * @param <V> The value type.
 */
public interface Registry<K, V> extends Map<K, V> {

}
